# 🚀 LeetCode Hot 100 极速通关手册 (详细注释版)

这份文档专为**快速掌握**设计。
我们不讲复杂的数学证明，只用**生活中的比喻**帮你建立直觉，配合**详细注释的代码**，让你看一遍就懂，写一遍就过。

**🔥 频度说明**：
*   🔥🔥🔥：面试必考，闭着眼睛都要会写。
*   🔥🔥：非常高频，核心算法题。
*   🔥：常见题目，需要掌握思路。

---

## 目录 (点击跳转)

### 第一章：哈希表 (Hash Map) - "查字典"
1.  [1. 两数之和](#1-1-两数之和-🔥🔥🔥)
2.  [49. 字母异位词分组](#2-49-字母异位词分组-🔥🔥)
3.  [128. 最长连续序列](#3-128-最长连续序列-🔥🔥)

### 第二章：双指针 (Two Pointers) - "左右夹击"
4.  [283. 移动零](#4-283-移动零-🔥🔥)
5.  [11. 盛最多水的容器](#5-11-盛最多水的容器-🔥🔥🔥)
6.  [15. 三数之和](#6-15-三数之和-🔥🔥🔥)
7.  [42. 接雨水](#7-42-接雨水-🔥🔥🔥)

### 第三章：滑动窗口 (Sliding Window) - "伸缩视窗"
8.  [3. 无重复字符的最长子串](#8-3-无重复字符的最长子串-🔥🔥🔥)
9.  [438. 找到字符串中所有字母异位词](#9-438-找到字符串中所有字母异位词-🔥🔥)

### 第四章：链表 (Linked List) - "穿针引线"
10.  [160. 相交链表](#10-160-相交链表-🔥🔥)
11.  [206. 反转链表](#11-206-反转链表-🔥🔥🔥)
12.  [234. 回文链表](#12-234-回文链表-🔥🔥)
13.  [141. 环形链表](#13-141-环形链表-🔥🔥🔥)
14.  [21. 合并两个有序链表](#14-21-合并两个有序链表-🔥🔥🔥)
15.  [19. 删除链表的倒数第 N 个结点](#15-19-删除链表的倒数第-n-个结点-🔥🔥)
16.  [146. LRU 缓存](#16-146-lru-缓存-🔥🔥🔥)

### 第五章：栈 (Stack) - "后进先出"
17.  [20. 有效的括号](#17-20-有效的括号-🔥🔥🔥)
18.  [155. 最小栈](#18-155-最小栈-🔥🔥)
19.  [394. 字符串解码](#19-394-字符串解码-🔥🔥)
20.  [739. 每日温度](#20-739-每日温度-🔥🔥)

### 第六章：堆 (Heap) - "优先级排队"
21.  [215. 数组中的第K个最大元素](#21-215-数组中的第k个最大元素-🔥🔥🔥)
22.  [347. 前 K 个高频元素](#22-347-前-k-个高频元素-🔥🔥)
23.  [295. 数据流的中位数](#23-295-数据流的中位数-🔥🔥🔥)

### 第七章：二叉树 (Binary Tree) - "分而治之"
24.  [104. 二叉树的最大深度](#24-104-二叉树的最大深度-🔥🔥)
25.  [226. 翻转二叉树](#25-226-翻转二叉树-🔥🔥🔥)
26.  [101. 对称二叉树](#26-101-对称二叉树-🔥🔥)
27.  [543. 二叉树的直径](#27-543-二叉树的直径-🔥🔥)
28.  [102. 二叉树的层序遍历](#28-102-二叉树的层序遍历-🔥🔥🔥)
29.  [105. 从前序与中序遍历序列构造二叉树](#29-105-从前序与中序遍历序列构造二叉树-🔥🔥)
30.  [236. 二叉树的最近公共祖先](#30-236-二叉树的最近公共祖先-🔥🔥🔥)

### 第八章：图论 (Graph) - "连接与搜索"
31.  [200. 岛屿数量](#31-200-岛屿数量-🔥🔥🔥)
32.  [994. 腐烂的橘子](#32-994-腐烂的橘子-🔥🔥)
33.  [207. 课程表](#33-207-课程表-🔥🔥🔥)
34.  [208. 实现 Trie (前缀树)](#34-208-实现-trie-前缀树-🔥🔥)

### 第九章：回溯 (Backtracking) - "不撞南墙不回头"
35.  [46. 全排列](#35-46-全排列-🔥🔥🔥)
36.  [78. 子集](#36-78-子集-🔥🔥🔥)
37.  [17. 电话号码的字母组合](#37-17-电话号码的字母组合-🔥🔥)
38.  [39. 组合总和](#38-39-组合总和-🔥🔥)
39.  [22. 括号生成](#39-22-括号生成-🔥🔥)
40.  [79. 单词搜索](#40-79-单词搜索-🔥🔥)
41.  [131. 分割回文串](#41-131-分割回文串-🔥🔥)
42.  [51. N 皇后](#42-51-n-皇后-🔥🔥🔥)

### 第十章：二分查找 (Binary Search) - "一分为二"
43.  [704. 二分查找](#43-704-二分查找-🔥)
44.  [35. 搜索插入位置](#44-35-搜索插入位置-🔥)
45.  [74. 搜索二维矩阵](#45-74-搜索二维矩阵-🔥🔥)
46.  [34. 在排序数组中查找元素的第一个和最后一个位置](#46-34-在排序数组中查找元素的第一个和最后一个位置-🔥🔥)
47.  [33. 搜索旋转排序数组](#47-33-搜索旋转排序数组-🔥🔥🔥)
48.  [153. 寻找旋转排序数组中的最小值](#48-153-寻找旋转排序数组中的最小值-🔥🔥)
49.  [4. 寻找两个正序数组的中位数](#49-4-寻找两个正序数组的中位数-🔥🔥🔥)

### 第十一章：贪心 (Greedy) - "目光短浅"
50.  [121. 买卖股票的最佳时机](#50-121-买卖股票的最佳时机-🔥)
51.  [55. 跳跃游戏](#51-55-跳跃游戏-🔥🔥)
52.  [45. 跳跃游戏 II](#52-45-跳跃游戏-ii-🔥🔥)
53.  [763. 划分字母区间](#53-763-划分字母区间-🔥🔥)

### 第十二章：动态规划 (Dynamic Programming) - "步步为营"
54.  [70. 爬楼梯](#54-70-爬楼梯-🔥)
55.  [118. 杨辉三角](#55-118-杨辉三角-🔥)
56.  [198. 打家劫舍](#56-198-打家劫舍-🔥🔥)
57.  [279. 完全平方数](#57-279-完全平方数-🔥🔥)
58.  [322. 零钱兑换](#58-322-零钱兑换-🔥🔥)
59.  [139. 单词拆分](#59-139-单词拆分-🔥🔥)
60.  [300. 最长递增子序列](#60-300-最长递增子序列-🔥🔥🔥)
61.  [416. 分割等和子集](#61-416-分割等和子集-🔥🔥)
62.  [32. 最长有效括号](#62-32-最长有效括号-🔥🔥)
63.  [62. 不同路径](#63-62-不同路径-🔥🔥)
64.  [64. 最小路径和](#64-64-最小路径和-🔥🔥)
65.  [5. 最长回文子串](#65-5-最长回文子串-🔥🔥)
66.  [1143. 最长公共子序列](#66-1143-最长公共子序列-🔥🔥)
67.  [72. 编辑距离](#67-72-编辑距离-🔥🔥🔥)

---

## 第一章：哈希表 (Hash Map)

### 1. [1] 两数之和 🔥🔥🔥
*   **通俗理解**：**相亲登记**。
    *   你拿着号码牌 `x` 进场，目标是凑成 `target`。
    *   你去登记处（哈希表）查：“有没有一个叫 `target - x` 的人来过？”
    *   **有**：配对成功，带走。
    *   **没有**：把自己的号码 `x` 和座位号 `i` 登记在表上，等有缘人。
*   **核心逻辑**：利用哈希表 $O(1)$ 的查找能力，把双重循环的 $O(N^2)$ 降为 $O(N)$。

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        # 字典：key是数字，value是这个数字的下标
        dic = {}
        
        for i, num in enumerate(nums):
            # 算出我想找的那个伴侣是谁
            need = target - num
            
            # 查字典：伴侣之前来过吗？
            if need in dic:
                # 找到了！返回伴侣的下标和我的下标
                return [dic[need], i]
            
            # 没找到，把我自己登记上去，等后面的人来找我
            dic[num] = i
            
        return []
```

### 2. [49] 字母异位词分组 🔥🔥
*   **通俗理解**：**卸妆认亲**。
    *   "eat", "tea", "ate" 看起来不一样，但它们“卸妆”（排序）后都是 "aet"。
    *   我们把“卸妆”后长得一样的单词，都拉到一个群里。
*   **核心逻辑**：
    *   Key：排序后的字符串（标准照）。
    *   Value：原单词列表（所有变体）。

```python
class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        # 使用 defaultdict，省去初始化列表的麻烦
        dic = collections.defaultdict(list)
        
        for s in strs:
            # 1. 卸妆：把单词按字母排序
            # 注意：sorted(s) 返回的是列表 ['a', 'e', 't']，需要 join 成字符串 "aet" 才能做 key
            key = "".join(sorted(s))
            
            # 2. 归队：把原单词 s 加入到对应的 key 下面
            dic[key].append(s)
            
        # 3. 返回所有分组
        return list(dic.values())
```

### 3. [128] 最长连续序列 🔥🔥
*   **通俗理解**：**拼图找龙头**。
    *   一堆数字乱糟糟的。我们只关心**“龙头”**（序列的起点）。
    *   如果一个数字是 `4`，且 `3` 也在堆里，那 `4` 肯定不是龙头（它前面有人），直接跳过。
    *   如果 `3` 不在，那 `4` 就是龙头！从 `4` 开始往后数 `5, 6, 7...` 能数多远数多远。
*   **核心逻辑**：先用 `Set` 去重（查找快）。只对“起点”元素开始计数，避免重复计算。

```python
class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:
        # 1. 先去重，并放入集合，实现 O(1) 查找
        num_set = set(nums)
        longest = 0
        
        for num in num_set:
            # 2. 只有当 num 是"龙头"时，才开始数
            # (如果 num-1 在集合里，说明 num 只是中间的一环，不是起点，跳过)
            if num - 1 not in num_set:
                current_num = num
                current_len = 1
                
                # 3. 拼命往后数
                while current_num + 1 in num_set:
                    current_num += 1
                    current_len += 1
                
                # 4. 记录最长的一次
                longest = max(longest, current_len)
                
        return longest
```

---

## 第二章：双指针 (Two Pointers)

### 4. [283] 移动零 🔥🔥
*   **通俗理解**：**滚雪球**。
    *   `slow` 指针代表“非零元素的队伍末尾”。
    *   `fast` 指针去前面探路。
    *   遇到非零的，就把它扔给 `slow`，`slow` 往前走一步。
    *   遇到零，`fast` 直接跨过去（不管它，相当于把零滚到了后面）。
*   **核心逻辑**：一次遍历，把非零元素按顺序挤到数组前面。

```python
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        slow = 0 # 慢指针：指向当前已经处理好的非零序列的下一个位置
        
        for fast in range(len(nums)):
            # 遇到非零元素
            if nums[fast] != 0:
                # 把它交换到 slow 的位置
                nums[slow], nums[fast] = nums[fast], nums[slow]
                # slow 往前挪，准备接下一个非零元素
                slow += 1
```

### 5. [11] 盛最多水的容器 🔥🔥🔥
*   **通俗理解**：**短板效应**。
    *   两条线组成一个桶，水能装多少，取决于**短**的那条线。
    *   如果我想让水装得更多，移动**长**的那条线是没用的（因为宽度变小了，高度受限于短板，面积只会变小）。
    *   **唯一的机会**是移动**短**的那条线，说不定能换个高一点的板子回来。
*   **核心逻辑**：双指针向中间靠拢，每次移动较短的那一根。

```python
class Solution:
    def maxArea(self, height: List[int]) -> int:
        left, right = 0, len(height) - 1
        res = 0
        
        while left < right:
            # 1. 计算当前面积：底 * 高 (短板)
            current_height = min(height[left], height[right])
            current_width = right - left
            res = max(res, current_height * current_width)
            
            # 2. 策略：谁矮谁动
            # (因为移动高板，宽度减小，高度上限还是被矮板限制，面积一定变小)
            if height[left] < height[right]:
                left += 1
            else:
                right -= 1
                
        return res
```

### 6. [15] 三数之和 🔥🔥🔥
*   **通俗理解**：**天平配重**。
    *   先**排序**（从小到大），这是关键。
    *   先选定一个砝码 `A`（固定 `i`）。
    *   然后在剩下的砝码里，找两个砝码 `B` 和 `C`（双指针 `left`, `right`），让 `B + C = -A`。
    *   如果轻了，`B` 换个大的（`left` 右移）；如果重了，`C` 换个小的（`right` 左移）。
*   **核心逻辑**：排序 + 固定一位 + 双指针。注意**去重**是难点。

```python
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums.sort() # 必须排序！
        res = []
        n = len(nums)
        
        for i in range(n - 2):
            # 优化：如果老大都大于0，后面肯定凑不出0了
            if nums[i] > 0: break
            # 去重：如果这个老大和上一个一样，跳过，避免重复结果
            if i > 0 and nums[i] == nums[i-1]: continue
            
            left, right = i + 1, n - 1
            while left < right:
                total = nums[i] + nums[left] + nums[right]
                
                if total < 0:
                    left += 1 # 太小了，左指针右移变大
                elif total > 0:
                    right -= 1 # 太大了，右指针左移变小
                else:
                    # 找到了！
                    res.append([nums[i], nums[left], nums[right]])
                    
                    # 去重：跳过重复的老二和老三
                    while left < right and nums[left] == nums[left+1]: left += 1
                    while left < right and nums[right] == nums[right-1]: right -= 1
                    
                    # 找到一组后，继续往中间缩，找下一组
                    left += 1
                    right -= 1
        return res
```

### 7. [42] 接雨水 🔥🔥🔥
*   **通俗理解**：**左右护法**。
    *   站在任何一个位置，头顶能存多少水，取决于**左边最高的墙**和**右边最高的墙**里，**较矮**的那一个。
    *   我们维护两个指针 `left` 和 `right`，以及两个变量 `left_max` 和 `right_max`。
    *   如果 `left_max < right_max`，说明左边是短板。那左边当前格子的水量就由 `left_max` 决定（不用管右边多高，反正左边漏水）。
*   **核心逻辑**：双指针，动态维护左右最高墙。

```python
class Solution:
    def trap(self, height: List[int]) -> int:
        left, right = 0, len(height) - 1
        left_max, right_max = 0, 0
        ans = 0
        
        while left < right:
            # 比较两边的墙，决定处理哪一边
            if height[left] < height[right]:
                # --- 处理左边 ---
                # 如果当前墙比左边历史最高还高，那它存不住水，更新最高墙
                if height[left] >= left_max:
                    left_max = height[left]
                else:
                    # 否则，它能存水 = 左边最高 - 当前高度
                    ans += left_max - height[left]
                left += 1
            else:
                # --- 处理右边 ---
                if height[right] >= right_max:
                    right_max = height[right]
                else:
                    ans += right_max - height[right]
                right -= 1
        return ans
```

---

## 第三章：滑动窗口 (Sliding Window)

### 8. [3] 无重复字符的最长子串 🔥🔥🔥
*   **通俗理解**：**橡皮筋**。
    *   `right` 拼命往右拉（进窗口）。
    *   一旦进来的字符**重复**了（在窗口里有了），`left` 就被迫往右缩（出窗口）。
    *   直到把那个重复的字符“吐”出去为止，`right` 才能继续拉。
*   **核心逻辑**：维护一个无重复元素的窗口。

```python
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        window = set() # 记录窗口里有哪些字符
        left = 0
        res = 0
        
        for right in range(len(s)):
            # 如果新来的字符 s[right] 已经在窗口里了，说明重复了
            # 左边就要一直缩，直到把那个重复的字符移出去
            while s[right] in window:
                window.remove(s[left])
                left += 1
            
            # 现在安全了，把新字符加进来
            window.add(s[right])
            # 每次都更新最大长度
            res = max(res, right - left + 1)
            
        return res
```

### 9. [438] 找到字符串中所有字母异位词 🔥🔥
*   **通俗理解**：**固定框框**。
    *   拿一个长度固定的框（长度等于目标单词 `p`），在长字符串 `s` 上滑。
    *   每滑一步，进一个字母，出一个字母。
    *   如果框里的字母统计（比如 1个a, 2个b）和 `p` 一样，就记下来。
*   **核心逻辑**：固定长度滑动窗口 + 数组计数（比 Map 快）。

```python
class Solution:
    def findAnagrams(self, s: str, p: str) -> List[int]:
        n, m = len(s), len(p)
        if n < m: return []
        
        # 用数组记录字符出现的次数 (a-z 26个)
        p_count = [0] * 26
        s_count = [0] * 26
        
        # 1. 初始化：先把 p 统计好，再把 s 的前 m 个统计好
        for i in range(m):
            p_count[ord(p[i]) - 97] += 1
            s_count[ord(s[i]) - 97] += 1
            
        res = []
        # 检查起点是否匹配
        if s_count == p_count:
            res.append(0)
        
        # 2. 开始滑动
        for i in range(m, n):
            # 进一个：s[i]
            s_count[ord(s[i]) - 97] += 1
            # 出一个：s[i-m] (窗口最左边那个)
            s_count[ord(s[i-m]) - 97] -= 1
            
            # 比较计数器是否一致
            if s_count == p_count:
                res.append(i - m + 1)
                
        return res
```

---

## 第四章：链表 (Linked List) - "穿针引线"

### 10. [160] 相交链表 🔥🔥
*   **通俗理解**：**你的名字**。
    *   两人在不同的跑道（链表）上跑。
    *   A 跑完自己的路，去跑 B 的路；B 跑完自己的路，去跑 A 的路。
    *   如果他们有缘（相交），因为 `len(A) + len(B) == len(B) + len(A)`，他们最后一定会在终点（交点）相遇。
*   **核心逻辑**：双指针遍历，消除长度差。

```python
class Solution:
    def getIntersectionNode(self, headA: ListNode, headB: ListNode) -> Optional[ListNode]:
        if not headA or not headB: return None
        pA, pB = headA, headB
        
        # 只要没相遇，就一直跑
        while pA != pB:
            # A 跑完了去跑 B，否则跑下一步
            pA = pA.next if pA else headB
            # B 跑完了去跑 A，否则跑下一步
            pB = pB.next if pB else headA
            
        return pA # 相遇点，或者都是 None (不相交)
```

### 11. [206] 反转链表 🔥🔥🔥
*   **通俗理解**：**向后转**。
    *   一排人手搭肩膀。
    *   你（Curr）松开前面人（Next）的肩膀，转身搭住后面人（Pre）的肩膀。
    *   然后大家整体往前挪一步，重复这个动作。
*   **核心逻辑**：双指针迭代，保存 `next`，修改 `next`，移动 `pre` 和 `curr`。

```python
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        pre = None
        curr = head
        while curr:
            next_node = curr.next # 1. 存下个（防止断链）
            curr.next = pre       # 2. 指前个（反转）
            pre = curr            # 3. 挪前个
            curr = next_node      # 4. 挪下个
        return pre
```

### 12. [234] 回文链表 🔥🔥
*   **通俗理解**：**对折重合**。
    *   先找到中点（快慢指针），把后半段切下来。
    *   把后半段**反转**一下。
    *   然后拿前半段和反转后的后半段，一个一个比。
*   **核心逻辑**：快慢指针找中点 + 反转链表。

```python
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        # 1. 快慢指针找中点
        slow, fast = head, head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            
        # 2. 反转后半部分 (slow 是中点)
        prev = None
        curr = slow
        while curr:
            temp = curr.next
            curr.next = prev
            prev = curr
            curr = temp
            
        # 3. 比较前半部分和反转后的后半部分
        while prev: # prev 是后半部分的头
            if head.val != prev.val:
                return False
            head = head.next
            prev = prev.next
            
        return True
```

### 13. [141] 环形链表 🔥🔥🔥
*   **通俗理解**：**操场套圈**。
    *   两个人跑步，一个跑得快（一次2步），一个跑得慢（一次1步）。
    *   如果是直线跑道，快的肯定直接到终点了。
    *   如果是环形跑道，快的迟早会**从后面追上**慢的（套圈）。
*   **核心逻辑**：快慢指针。

```python
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        slow, fast = head, head
        while fast and fast.next:
            slow = slow.next       # 慢走1步
            fast = fast.next.next  # 快走2步
            if slow == fast:       # 相遇了
                return True
        return False
```

### 14. [21] 合并两个有序链表 🔥🔥🔥
*   **通俗理解**：**拉链**。
    *   两个有序队伍合并成一个。
    *   谁小谁先走，然后那个队伍的下一个人补上来继续比。
    *   最后如果哪个队伍还有剩的，直接全部接在后面。
*   **核心逻辑**：虚拟头节点 (`dummy`) + 双指针。

```python
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        dummy = ListNode(-1) # 虚拟头节点，方便处理
        curr = dummy
        
        while list1 and list2:
            if list1.val <= list2.val:
                curr.next = list1
                list1 = list1.next
            else:
                curr.next = list2
                list2 = list2.next
            curr = curr.next
            
        # 接上剩下的
        curr.next = list1 if list1 else list2
        
        return dummy.next
```

### 15. [19] 删除链表的倒数第 N 个结点 🔥🔥
*   **通俗理解**：**尺子量**。
    *   我们要找倒数第 N 个，但我们不知道终点在哪。
    *   让快指针先走 N 步，拉开一把长为 N 的“尺子”。
    *   然后快慢指针一起走，当快指针走到终点时，慢指针刚好就在被删节点的前面（因为它们之间隔了 N 步）。
*   **核心逻辑**：快慢指针保持固定间距。

```python
class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
        dummy = ListNode(0, head)
        slow, fast = dummy, dummy
        
        # 1. 快指针先走 n+1 步 (为了让 slow 停在被删节点的前一个)
        for _ in range(n + 1):
            fast = fast.next
            
        # 2. 一起走，直到 fast 到头
        while fast:
            slow = slow.next
            fast = fast.next
            
        # 3. 删除 slow 后面的那个节点
        slow.next = slow.next.next
        
        return dummy.next
```

### 16. [146] LRU 缓存 🔥🔥🔥
*   **通俗理解**：**书架管理**。
    *   书架只能放 K 本书。
    *   **看书**：不管书在哪，抽出来插到最前面（最近刚读过）。
    *   **进新书**：插到最前面。如果书架满了，把最后面那本（最久没读的）扔掉。
*   **核心逻辑**：哈希表（查书快）+ 双向链表（挪书快）。

```python
class LRUCache:
    def __init__(self, capacity: int):
        self.dic = collections.OrderedDict() # Python 有序字典 = 哈希 + 双向链表
        self.remain = capacity

    def get(self, key: int) -> int:
        if key not in self.dic:
            return -1
        # 读了这本书，把它抽出来插到最后（代表最近用过）
        v = self.dic.pop(key) 
        self.dic[key] = v
        return v

    def put(self, key: int, value: int) -> None:
        if key in self.dic:
            self.dic.pop(key) # 如果有了，先拿出来
        else:
            if self.remain > 0:
                self.remain -= 1
            else:
                # 满了，扔掉最前面那个（最久没用的）
                # last=False 表示弹出第一个
                self.dic.popitem(last=False) 
        self.dic[key] = value # 插到最后
```

---

## 第十四章：技巧与数学 (Tricks & Math) - "脑筋急转弯"

### 69. [136] 只出现一次的数字 🔥
*   **通俗理解**：**消消乐**。
    *   两个相同的数异或等于 0 (`a ^ a = 0`)。
    *   0 和任何数异或等于那个数 (`0 ^ a = a`)。
    *   把所有数都异或一遍，成对的都消掉了，剩下的就是那个单身狗。
*   **核心逻辑**：位运算异或 (`^`)。

```python
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        res = 0
        for num in nums:
            res ^= num
        return res
```

### 70. [169] 多数元素 🔥
*   **通俗理解**：**打擂台**。
    *   假设第一个人是擂主，票数=1。
    *   后面的人，如果和擂主一伙，票数+1；如果不是一伙，票数-1。
    *   如果票数变成0，擂主下台，换当前这个人当擂主。
    *   因为多数元素超过一半，所以最后站在擂台上的肯定是他。
*   **核心逻辑**：摩尔投票法 (Boyer-Moore Voting Algorithm)。

```python
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        count = 0
        candidate = None
        for num in nums:
            if count == 0:
                candidate = num
            count += (1 if num == candidate else -1)
        return candidate
```

### 71. [75] 颜色分类 🔥🔥
*   **通俗理解**：**三路快排**。
    *   三个指针：`p0` (放0的边界)，`p2` (放2的边界)，`curr` (当前遍历)。
    *   遇到 0，扔到 `p0` 左边；遇到 2，扔到 `p2` 右边；遇到 1，不管，继续走。
*   **核心逻辑**：双指针（三指针）一次遍历。

```python
class Solution:
    def sortColors(self, nums: List[int]) -> None:
        p0, curr, p2 = 0, 0, len(nums) - 1
        while curr <= p2:
            if nums[curr] == 0:
                nums[p0], nums[curr] = nums[curr], nums[p0]
                p0 += 1
                curr += 1
            elif nums[curr] == 2:
                nums[curr], nums[p2] = nums[p2], nums[curr]
                p2 -= 1 # curr 不动，因为换回来的可能是 0 或 1，还要看
            else:
                curr += 1
```

### 72. [31] 下一个排列 🔥🔥🔥
*   **通俗理解**：**字典序变大一点点**。
    *   从后往前找，找到第一个“变小”的数 `nums[i]` (比如 1, 5, 8, 4, 7, 6, 5, 3, 1 中的 4)。
    *   再从后往前找，找到第一个比它大的数 `nums[j]` (是 5)。
    *   交换它俩 (变成 1, 5, 8, 5, 7, 6, 4, 3, 1)。
    *   把 `i` 后面的所有数反转（变成最小序列），保证变大的幅度最小。
*   **核心逻辑**：找拐点 -> 找大数 -> 交换 -> 反转。

```python
class Solution:
    def nextPermutation(self, nums: List[int]) -> None:
        i = len(nums) - 2
        # 1. 找第一个升序对 (i, i+1)
        while i >= 0 and nums[i] >= nums[i+1]:
            i -= 1
            
        if i >= 0:
            # 2. 找第一个比 nums[i] 大的数
            j = len(nums) - 1
            while j >= 0 and nums[j] <= nums[i]:
                j -= 1
            nums[i], nums[j] = nums[j], nums[i]
            
        # 3. 反转 i 之后的部分
        left, right = i + 1, len(nums) - 1
        while left < right:
            nums[left], nums[right] = nums[right], nums[left]
            left += 1
            right -= 1
```

### 73. [287] 寻找重复数 🔥🔥
*   **通俗理解**：**链表找环**。
    *   把数组的值看作 `next` 指针：`nums[i]` 指向 `nums[nums[i]]`。
    *   如果有重复数，说明有两个位置指向了同一个位置，这就形成了环。
    *   问题变成了“环形链表 II”：找到入环点。
*   **核心逻辑**：快慢指针 (Floyd 判圈算法)。

```python
class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        slow, fast = 0, 0
        while True:
            slow = nums[slow]
            fast = nums[nums[fast]]
            if slow == fast: break
            
        slow = 0
        while slow != fast:
            slow = nums[slow]
            fast = nums[fast]
        return slow
```

---

## 第十五章：数组与矩阵 (Array & Matrix) - "乾坤大挪移"

### 74. [48] 旋转图像 🔥🔥
*   **通俗理解**：**折纸**。
    *   顺时针旋转 90 度 = 先沿对角线翻转，再沿垂直中线翻转。
    *   或者：先上下翻转，再对角线翻转。
*   **核心逻辑**：矩阵变换。

```python
class Solution:
    def rotate(self, matrix: List[List[int]]) -> None:
        n = len(matrix)
        # 1. 上下翻转
        # matrix[:] = matrix[::-1] 
        for i in range(n // 2):
            matrix[i], matrix[n-1-i] = matrix[n-1-i], matrix[i]
            
        # 2. 对角线翻转
        for i in range(n):
            for j in range(i + 1, n):
                matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]
```

### 75. [54] 螺旋矩阵 🔥🔥
*   **通俗理解**：**剥洋葱**。
    *   设定四个边界：上、下、左、右。
    *   向右走，上边界下移；向下走，右边界左移；向左走，下边界上移；向上走，左边界右移。
    *   一直转圈直到边界相撞。
*   **核心逻辑**：模拟边界收缩。

```python
class Solution:
    def spiralOrder(self, matrix: List[List[int]]) -> List[int]:
        if not matrix: return []
        l, r, t, b = 0, len(matrix[0])-1, 0, len(matrix)-1
        res = []
        
        while True:
            # left -> right
            for i in range(l, r + 1): res.append(matrix[t][i])
            t += 1; 
            if t > b: break
            
            # top -> bottom
            for i in range(t, b + 1): res.append(matrix[i][r])
            r -= 1; 
            if l > r: break
            
            # right -> left
            for i in range(r, l - 1, -1): res.append(matrix[b][i])
            b -= 1; 
            if t > b: break
            
            # bottom -> top
            for i in range(b, t - 1, -1): res.append(matrix[i][l])
            l += 1; 
            if l > r: break
            
        return res
```

### 76. [238] 除自身以外数组的乘积 🔥🔥
*   **通俗理解**：**左右互搏**。
    *   `res[i]` = `i` 左边所有数的乘积 * `i` 右边所有数的乘积。
    *   先从左往右扫一遍，算出每个位置左边的乘积。
    *   再从右往左扫一遍，乘上右边的乘积。
*   **核心逻辑**：前缀积 + 后缀积。

```python
class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:
        n = len(nums)
        res = [1] * n
        
        # 左边的乘积
        left = 1
        for i in range(n):
            res[i] = left
            left *= nums[i]
            
        # 右边的乘积
        right = 1
        for i in range(n - 1, -1, -1):
            res[i] *= right
            right *= nums[i]
            
        return res
```

### 77. [56] 合并区间 🔥🔥
*   **通俗理解**：**贪吃蛇**。
    *   先把区间按起点排序。
    *   拿出第一个区间作为“当前区间”。
    *   看下一个区间：
        *   如果它的起点在“当前区间”里，说明重叠了，把“当前区间”变长（终点取最大值）。
        *   如果它的起点在外面，说明断开了，把“当前区间”存起来，开始一个新的“当前区间”.
*   **核心逻辑**：排序 + 贪心合并。

```python
class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        intervals.sort(key=lambda x: x[0])
        merged = []
        for interval in intervals:
            if not merged or merged[-1][1] < interval[0]:
                merged.append(interval)
            else:
                merged[-1][1] = max(merged[-1][1], interval[1])
        return merged
```

### 78. [73] 矩阵置零 🔥
*   **通俗理解**：**做记号**。
    *   如果 `matrix[i][j]` 是 0，那第 i 行和第 j 列都要变 0。
    *   我们可以用第一行和第一列来当“记事本”。
    *   如果 `matrix[i][j] == 0`，就把 `matrix[i][0]` 和 `matrix[0][j]` 设为 0。
    *   最后根据记事本把对应的行和列全变 0。
*   **核心逻辑**：原地标记 (O(1) 空间)。

```python
class Solution:
    def setZeroes(self, matrix: List[List[int]]) -> None:
        m, n = len(matrix), len(matrix[0])
        row0_has_zero = any(matrix[0][j] == 0 for j in range(n))
        col0_has_zero = any(matrix[i][0] == 0 for i in range(m))
        
        # 1. 标记
        for i in range(1, m):
            for j in range(1, n):
                if matrix[i][j] == 0:
                    matrix[i][0] = matrix[0][j] = 0
                    
        # 2. 置零 (从内向外)
        for i in range(1, m):
            for j in range(1, n):
                if matrix[i][0] == 0 or matrix[0][j] == 0:
                    matrix[i][j] = 0
                    
        # 3. 处理第一行第一列
        if row0_has_zero:
            for j in range(n): matrix[0][j] = 0
        if col0_has_zero:
            for i in range(m): matrix[i][0] = 0
```

### 79. [240] 搜索二维矩阵 II 🔥🔥
*   **通俗理解**：**走楼梯**。
    *   从右上角开始看。
    *   如果当前数 > target，说明这一列都太大了，往左移。
    *   如果当前数 < target，说明这一行都太小了，往下移。
*   **核心逻辑**：利用行列有序性，从右上角或左下角开始搜索。

```python
class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
        if not matrix: return False
        m, n = len(matrix), len(matrix[0])
        row, col = 0, n - 1 # 右上角
        
        while row < m and col >= 0:
            if matrix[row][col] == target:
                return True
            elif matrix[row][col] > target:
                col -= 1
            else:
                row += 1
        return False
```

---

## 第十六章：栈与单调栈进阶 (Stack Advanced)

### 80. [84] 柱状图中最大的矩形 🔥🔥🔥
*   **通俗理解**：**向两边延伸**。
    *   对于每一根柱子，我想知道以它为高度，最左能延伸到哪，最右能延伸到哪。
    *   边界就是“第一个比它矮的柱子”。
    *   用单调栈找到每个柱子左右两边第一个比它矮的位置。
*   **核心逻辑**：单调递增栈。

```python
class Solution:
    def largestRectangleArea(self, heights: List[int]) -> int:
        heights = [0] + heights + [0] # 哨兵，保证最后能弹出
        stack = []
        res = 0
        for i, h in enumerate(heights):
            while stack and h < heights[stack[-1]]:
                height = heights[stack.pop()]
                width = i - stack[-1] - 1
                res = max(res, height * width)
            stack.append(i)
        return res
```

### 81. [85] 最大矩形 🔥🔥🔥
*   **通俗理解**：**层层扫描**。
    *   把每一行看作是“柱状图的底座”。
    *   第一行是柱状图，算一次最大矩形（用上一题的方法）。
    *   第二行加上第一行，形成新的柱状图，再算一次。
    *   如果遇到 0，柱子高度归零。
*   **核心逻辑**：转化为 [84] 题。

```python
class Solution:
    def maximalRectangle(self, matrix: List[List[str]]) -> int:
        if not matrix: return 0
        n = len(matrix[0])
        heights = [0] * n
        res = 0
        
        for row in matrix:
            for i in range(n):
                # 如果是1，高度+1；如果是0，高度归零
                heights[i] = heights[i] + 1 if row[i] == '1' else 0
            # 复用上一题的逻辑
            res = max(res, self.largestRectangleArea(heights))
        return res

    def largestRectangleArea(self, heights):
        # ...同上题代码...
        heights = [0] + heights + [0]
        stack = []
        res = 0
        for i, h in enumerate(heights):
            while stack and h < heights[stack[-1]]:
                height = heights[stack.pop()]
                width = i - stack[-1] - 1
                res = max(res, height * width)
            stack.append(i)
        return res
```